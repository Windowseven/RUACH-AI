# RUACH model registry
#
# All byte figures below are PROVISIONAL ESTIMATES for candidate evaluation.
# Per Amendment 1: entries remain status = "experimental" until RUACH
# benchmarks establish load time, memory consumption, tokens/sec, stability
# and usability on target hardware (doc 10 §6 template).
#
# DECLARATION ORDER IS PREFERENCE: the recommendation engine picks the first
# entry that fits the safe memory budget. List larger/better-quality models
# first so capable devices get the strongest candidate and weaker devices
# fall through to smaller ones.
#
# sha256 is filled in after the FIRST checksum-verified download; an empty
# value means "not yet verified" and blocks installation in later increments.

[models."qwen3-4b"]
family = "qwen3"
parameters = "4B"
format = "gguf"
quantization = "Q4_K_M"
file_name = "Qwen3-4B-Q4_K_M.gguf"
download_size_bytes = 2500000000       # ~2.4 GiB provisional
estimated_memory_bytes = 3000000000    # weights + KV cache estimate, provisional
min_recommended_memory_bytes = 5000000000
max_context_tokens = 8192
quality_tier = "balanced"
speed_expectation = "requires high-memory device; expect slow generation on mobile SoCs"
status = "experimental"
source_url = "https://huggingface.co/Qwen/Qwen3-4B-GGUF/resolve/main/Qwen3-4B-Q4_K_M.gguf"
sha256 = ""

[models."qwen3-0.6b"]
family = "qwen3"
parameters = "0.6B"
format = "gguf"
quantization = "Q8_0"
file_name = "Qwen3-0.6B-Q8_0.gguf"
download_size_bytes = 639446688        # measured on real download (2026-08-23)
estimated_memory_bytes = 900000000     # weights + KV cache estimate, provisional
min_recommended_memory_bytes = 1200000000
max_context_tokens = 8192              # conservative operating ceiling
quality_tier = "light"
speed_expectation = "interactive on mid phones; slow on efficiency cores"
status = "experimental"
source_url = "https://huggingface.co/Qwen/Qwen3-0.6B-GGUF/resolve/main/Qwen3-0.6B-Q8_0.gguf"
sha256 = "9465e63a22add5354d9bb4b99e90117043c7124007664907259bd16d043bb031"
