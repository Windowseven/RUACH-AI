name: Build ARM32 Binary v2

on:
  workflow_dispatch:

jobs:
  build-arm32:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install cross-compiler
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq cmake g++-arm-linux-gnueabihf
      - name: Clone and build llama.cpp
        run: |
          git clone --depth 1 --branch b10622 https://github.com/ggml-org/llama.cpp.git /tmp/llama.cpp
          cd /tmp/llama.cpp
          cmake -B build \
            -DCMAKE_SYSTEM_NAME=Linux \
            -DCMAKE_SYSTEM_PROCESSOR=armv7l \
            -DCMAKE_C_COMPILER=arm-linux-gnueabihf-gcc \
            -DCMAKE_CXX_COMPILER=arm-linux-gnueabihf-g++ \
            -DCMAKE_BUILD_TYPE=Release \
            -DLLAMA_CURL=OFF \
            -DGGML_NATIVE=OFF
          cmake --build build --config Release -j$(nproc)
          echo "=== Build results ==="
          ls -lh build/bin/llama-server
          file build/bin/llama-server
          ls -lh build/bin/lib*.so* 2>/dev/null | head -20
      - name: Package all files
        run: |
          cd /tmp/llama.cpp/build/bin
          tar -czf /tmp/llama-server-arm-linux.tar.gz llama-server lib*.so*
          ls -lh /tmp/llama-server-arm-linux.tar.gz
          tar -tzf /tmp/llama-server-arm-linux.tar.gz | head -20
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: llama-server-arm-linux
          path: /tmp/llama-server-arm-linux.tar.gz
